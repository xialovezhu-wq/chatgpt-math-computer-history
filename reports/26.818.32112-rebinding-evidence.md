# ChatGPT Math 26.818.32112 rebind evidence

日期：2026-08-22

状态：`PASS_REBINDING`

## 1. 范围与基线

- 分支：`codex/update-math-copy-26.818.32112-build`
- 起点：`71d7e9e7da3adc3cfb6c8b45883051f74f89a0ee`
- 官方版本：26.818.32112，build 6933
- Bundle ID：`com.openai.codex`
- 官方 ASAR SHA-256：`128c748e313a7a630d689f9fa215724eb44fbea6e0a5d7990867370cf73d88d3`
- 官方 header JSON SHA-256：`f86ad032ad7481f888bb0930a7298bd534e84e0a6f9a22545bc056e203603bd6`
- header round trip：PASS

本报告只记录路径、哈希、计数和短语义标记，不保存完整官方 entry 源码。

## 2. 审计矩阵修正

只读审计报告原记载为 13/20 保留、7/20 变化。重新以仓库旧构建器的 exact string 为事实源复算后，发现 Electron main 中 `setEnabled` 的错误归一化压缩别名已由旧快照的 `n.Un` 变化为新版的 `n.Wn`。旧 exact anchor 在新版命中 0 次，新语义完整方法在目标 entry 命中 1 次。

因此最终矩阵是：

- 12/20 原锚点保留；
- 8/20 重新绑定；
- 20/20 最终唯一绑定。

这项修正仍位于已批准的 `.vite/build/main-B2sRTTQY.js` 和既有 `setEnabled` 方法内，不增加 entry，不扩大代理能力。

## 3. 新版 entry 锁定

| 角色 | 新版 entry | 原始 SHA-256 | 大小 |
|---|---|---|---:|
| 数学扩展 | `webview/assets/user-formatted-text-BtKDcaQ7.js` | `165905ef043e51618af9483670ef09653b014bd623ff6fab1c9b07b4aaa0ce91` | 3,125 |
| 用户气泡和浮层注释 | `webview/assets/subagent-activity-chip-group-BL5rmF0-.js` | `d4c5a3f52b45c59bf0533ca73b63418434bbb699c3a96ddd2af4b74ef80e837d` | 557,654 |
| 已保存注释 | `webview/assets/app-initial-CanCbU9v.js` | `364e244fdb6b17e4d3a1de0413402284e62595456cc973cf15d35bb21fc11a76` | 14,640,847 |
| Electron main / History | `.vite/build/main-B2sRTTQY.js` | `a38a92eaac29e375fa843e3e7c0c2016bd8f28f7e0f6143c75a079359da4ddcd` | 2,685,867 |

四个 entry 的实际 whole-entry hash 均与 header integrity hash 相同。

## 4. 20 锚点最终矩阵

| 编号 | 模块 | 功能角色 | 绑定状态 | entry 命中 | 全 ASAR 命中 |
|---:|---|---|---|---:|---:|
| 1 | 数学扩展 | extension initializer | 保留 | 1 | 1 |
| 2 | 数学扩展 | variables | 保留 | 1 | 14 |
| 3 | 数学扩展 | gated extension selection | 保留 | 1 | 1 |
| 4 | 数学扩展 | formatter cache size | 保留 | 1 | 1 |
| 5 | 数学扩展 | annotation normalization cache | 保留 | 1 | 1 |
| 6 | 用户气泡 | user bubble Markdown call | 重新绑定 | 1 | 1 |
| 7 | 浮层注释 | selected text renderer | 保留 | 1 | 1 |
| 8 | 浮层注释 | user comment renderer | 重新绑定 | 1 | 1 |
| 9 | 浮层注释 | formatted-text dependency | 重新绑定 | 1 | 1 |
| 10 | 已保存注释 | initializer | 重新绑定 | 1 | 1 |
| 11 | 已保存注释 | renderer | 重新绑定 | 1 | 1 |
| 12 | 已保存注释 | base Markdown dependency | 重新绑定 | 1 | 1 |
| 13 | History | retryActivation | 保留 | 1 | 1 |
| 14 | History | setEnabled | 重新绑定 | 1 | 1 |
| 15 | History | getState | 保留 | 1 | 1 |
| 16 | History | pause | 保留 | 1 | 1 |
| 17 | History | resume | 保留 | 1 | 1 |
| 18 | History | getSettings | 保留 | 1 | 1 |
| 19 | History | updateSettings | 保留 | 1 | 1 |
| 20 | History | helper insertion point | 重新绑定 | 1 | 1 |

编号 2 的短变量声明在整包中并不唯一，但它位于已由独立 entry hash 锁定的 3,125-byte 数学扩展 entry 内，且同一 entry 的 initializer、selection、cache 和 normalization 四个语义锚点均唯一。构建器不以该短变量声明单独认定功能位置。

## 5. 八个重新绑定项

### 5.1 用户气泡 Markdown 调用

- 旧 entry：`subagent-activity-chip-group-BMNUltzO.js`
- 新 entry：`subagent-activity-chip-group-BL5rmF0-.js`
- 新位置：用户消息格式化函数内，目标调用把 `cwd`、`hostId`、`externalLinkContextMenuConversationId` 和用户文本传给新版 formatted-text component。
- 独立语义标记：`externalLinkContextMenuConversationId` 全 entry 1 次；同函数同时包含 `collapsedLineCount` 和 user-message text wrapper。
- 负面排除：assistant role、工具输出和普通 Markdown renderer 不具有该完整 props 组合。
- 替换前计数：1。
- 替换后预期：旧调用 0；`markdownClassName:codex-single-dollar-math` 1。
- 证据等级：高。
- 未解决风险：实际 React DOM 行为仍需后续运行时验收。

### 5.2 浮层用户评论 renderer

- 旧 entry：`subagent-activity-chip-group-BMNUltzO.js`
- 新 entry：`subagent-activity-chip-group-BL5rmF0-.js`
- 新位置：response annotation tooltip 内 `annotation?.trim()` 条件分支。
- 独立语义标记：`responseTextAnnotationAttachments.userCommentLabel` 语义标签；`annotation?.trim()` 条件；相邻 selected-text aria label。
- 负面排除：同 class pattern 的其他 div 不同时满足 user-comment label 和 annotation field。
- 替换前计数：1。
- 替换后预期：旧 plain renderer 0；annotation formatted-text renderer 1。
- 证据等级：高。
- 未解决风险：tooltip 高度和滚动表现属于运行时未知项。

### 5.3 浮层 formatted-text dependency

- 旧 entry：`subagent-activity-chip-group-BMNUltzO.js`
- 新 entry：`subagent-activity-chip-group-BL5rmF0-.js`
- 新位置：response annotation directive 模块 initializer。
- 独立语义标记：模块常量 `codex-annotation`；同模块含 annotation lexer/aria 处理；entry 唯一导入新版 `user-formatted-text`，其 initializer alias 与 component alias成对导入。
- 负面排除：用户气泡自身的 initializer 已调用 formatted-text initializer；本锚点专门覆盖可独立初始化的 annotation 模块。
- 替换前计数：1。
- 替换后预期：旧 initializer 0；增加 formatted-text initializer 的新 initializer 1。
- 证据等级：高。
- 未解决风险：模块 lazy-init 顺序只能静态证明依赖调用存在，运行时仍需 UI 验收。

### 5.4 已保存注释 initializer

- 旧 entry：`app-initial-BCLYDefw.js`，旧函数 `jhc`。
- 新 entry：`app-initial-CanCbU9v.js`，新函数 `TAc`。
- 新位置：接收 `label` 和 `value` 的 7-slot React cache component。
- 独立语义标记：`label/value` 解构；同函数内唯一 saved plain renderer；下游 composer annotation selected-content 与 user-comment labels 调用该 component。
- 负面排除：其他 `responseAnnotation` 字样属于数据模型或 assistant-message conversion，不具有该 label/value renderer 结构。
- 替换前计数：1。
- 替换后预期：注入的 extension list 和 normalizer 各 1 个定义；原函数仍 1。
- 证据等级：高。
- 未解决风险：运行时文本测量和样式仍未验收。

### 5.5 已保存注释 renderer

- 旧 entry：`app-initial-BCLYDefw.js`。
- 新 entry：`app-initial-CanCbU9v.js`。
- 新位置：`TAc` 的 value 渲染分支。
- 独立语义标记：唯一 `mt-0.5 break-words whitespace-pre-wrap` plain renderer；输入变量是 `value`；与 label renderer 共处同一 7-slot component。
- 负面排除：design-tweak、评论正文和其他 plain renderer 不在 `TAc`，也不接收该 `value`。
- 替换前计数：1。
- 替换后预期：plain renderer 0；base Markdown renderer 加 annotation extension 1。
- 证据等级：高。
- 未解决风险：实际 Markdown error boundary 和视觉样式属于运行时未知项。

### 5.6 已保存注释 base Markdown dependency

- 旧 entry：`app-initial-BCLYDefw.js`，旧 dependency alias `Woa`。
- 新 entry：`app-initial-CanCbU9v.js`，新版 base renderer 为 `zB`，其模块 initializer 为 `Dda`。
- 新位置：`DAc` initializer，即 `TAc` 的同一模块初始化器。
- 独立语义标记：`function zB` 具有 Markdown error boundary 和 81-slot Markdown worker；`Dda` 初始化其 cache、renderer、directive 与 Markdown 依赖；`zB` 作为 `eY` 导出给 formatted-text entry 使用。
- 负面排除：`M6s` 是 attachment UI initializer，不是 Markdown initializer；不能沿用旧报告对邻近函数名的猜测。
- 替换前计数：1。
- 替换后预期：`DAc` initializer 中增加 `Dda()`，且仅 1 次。
- 证据等级：高。
- 未解决风险：压缩内部符号仍是版本锁定实现，不能跨版本复用。

### 5.7 History setEnabled

- 旧 entry：`main-DkjTIhil.js`，旧 exact method 使用错误归一化别名 `n.Un`。
- 新 entry：`main-B2sRTTQY.js`，新版 exact method 使用 `n.Wn`。
- 新位置：目标 History 控制 class 的 `setEnabled`。
- 独立语义标记：调用 `enableSkysightChronicle`；保留 pending、denied、disable rollback 与状态映射；与 `getState`、retry、pause、resume 共处同一 class。
- 负面排除：另一个 `setEnabled` 属于 appshot hotkey controller，不含 Chronicle、pending/denied 或 rollback 语义。
- 替换前计数：1。
- 替换后预期：原 method 0；只增加显式 companion ensure，既有 enable/rollback 内容保持不变。
- 证据等级：高。
- 未解决风险：真实 enable 权限和 companion 生命周期未调用、未验收。

### 5.8 History helper insertion point

- 旧 entry：`main-DkjTIhil.js`，旧短 anchor `var bue=class`。
- 新 entry：`main-B2sRTTQY.js`。
- 新位置：完整结构 anchor 从 `var gue=class extends ...` 延伸到 controller fields、constructor 和 `getState` 开头；构建器不使用 `var gue=class` 单独替换。
- 独立语义标记：class fields 同时包含 app-server connection、controller、eligibility、applications loaders 和 history；class 内包含全部七个既有替换点及 list/resolve/history/clear/internal disable/reconcile 路径。
- 负面排除：`resumeChronicleSidecar` 全 entry 有 2 次。目标 occurrence 位于该 class 的唯一 `async resume()`；另一处位于 `toggleChronicleSidecar` 托盘动作，明确不修改。
- 替换前计数：完整结构 anchor 1；短变量线索 1。
- 替换后预期：完整结构 anchor 保留 1；helper 定义各 1；不得修改托盘 occurrence。
- 证据等级：高。
- 未解决风险：helper 的真实外部进程与 MCP 行为属于阶段 F 之后的运行时验收。

## 6. 五个 MCP 操作与七个替换点

五个 MCP 对外操作是：

| MCP 操作 | 替换方法 |
|---|---|
| `computer_history_status` | `getState` |
| `computer_history_pause` | `pause` |
| `computer_history_resume` | `resume` |
| `computer_history_get_settings` | `getSettings` |
| `computer_history_update_settings` | `updateSettings` |

七个实际方法替换点是：

1. `retryActivation`：只增加显式 companion ensure，不新增 MCP 操作。
2. `setEnabled`：只在 enable=true 路径增加显式 companion ensure；enable=false 仍走原 disable 边界。
3. `getState`：代理到 status MCP。
4. `pause`：代理到 pause MCP。
5. `resume`：代理到 resume MCP。
6. `getSettings`：代理到 get-settings MCP。
7. `updateSettings`：代理到 update-settings MCP。

因此“五个 MCP 操作”是七个方法替换点中的五项；另外两项是用户显式激活的 companion 前置动作。list、resolve、history、suggestions、summary intervals、clear 和内部 disable/reconcile 不扩大代理覆盖。

## 7. 计划中的替换后 sentinel

- `codex-single-dollar-math`：extension 1，bubble 1。
- `codex-annotation-math`：extension 2，bubble 2。
- `codexAnnotationMathExtensions`：annotation 2。
- `codexNormalizeAnnotationMath`：annotation 2。
- `codexMathHistoryCall`：main 6 次，其中 1 次定义、5 次调用。
- `codexMathEnsureHistoryCompanion`：main 3 次，其中 1 次定义、2 次调用。
- `--user-activate`：main 1。

这些是候选构建后的预期，不在本报告中提前声称已经生成或通过。

## 8. 结论与边界

绑定门禁通过：`PASS_REBINDING`。

当前只证明版本锁定的静态语义位置。没有生成候选 ASAR，没有写入 `/Applications`，没有修改或启动应用，没有调用 Computer History MCP，没有修改 LaunchAgent，没有签名、部署、push、PR 或 tag。
