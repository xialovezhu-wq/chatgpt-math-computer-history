# ChatGPT Math 26.818.61809 静态候选验证

日期：2026-08-25

状态：`PASS_STATIC_CANDIDATE`

## 候选锁

- Math ASAR SHA-256：`5eed54c90e683e2beb7c28de8770d57117ae5e694804563f13d9bcd89ce4efb6`
- Math header SHA-256：`940ad99700829aa669627b76f58ac4963bad85b46bb0eb454222680d15258dab`
- Final ASAR SHA-256：`20af3332ab0fbf396eee9be3cde8e61e61025eca8287a1f0388fab03aa5c8e29`
- Final header SHA-256：`624b1fe2cd85d9a2d3877f9b275112e9e35e718abbb9e500231e736350876f3e`
- Metadata change count：Math 3，Final 4。
- Reproducibility：至少两次完整语义构建哈希一致；冻结锁后由根任务再次完整构建并验证通过。

## 通过项

- 官方 26.818.61809／7019 的版本、ASAR、header 和四个目标 entry 锁全部匹配。
- 旧 20 项迁移锚点最终为 18 retained、2 rebound，20／20 唯一。
- 实际 15 个数学／预览补丁点为 10 retained、5 rebound，15／15 唯一。
- Preview cwd 已绑定到 selected-project root-path atom `bb`；构建器与验证器均拒绝 local-projects map atom `Sb`。
- 数学 tokenizer、annotation normalizer、React cache slot 和依赖初始化通过。
- 预览只延迟加载同一 `user-formatted-text` 组件，动态依赖闭包完整，未复制第二套数学扩展。
- 共享 `singleDollarTextMath: false` entry 与官方源逐字节相同，开启值为零。
- Assistant renderer 和 KaTeX 保护窗口与官方源保持不变。
- History 七个目标方法、五项 MCP、显式 activation、`setEnabled(false)`、list、clear 和 tray resume 边界通过。
- 共检查 8,561 个 entry：8,160 packed、401 unpacked、1 个 packed zero-byte。
- ASAR header round trip、官方 data prefix、whole-entry hash、block hash、unpacked 状态和 active-range overlap 全部通过。
- 三个数学／预览 entry 和一个 History entry 是唯一批准的元数据变化。

## 静态验证没有证明的内容

- 候选应用签名、AMFI 和真实启动。
- 实际窗口、renderer、登录账户和 app-server 连接。
- 预览动态 chunk 的运行时加载，以及预览和发送后气泡的 DOM 等价。
- 两类注释、焦点、撤销、输入法 composition 和错误公式的真实界面行为。
- Computer History MCP、显式 companion activation 和至少 315 秒暂停保护。
- 正式部署和已安装路径验证。

这些项目必须由后续 staging 签名检查、单独授权的 runtime receipt 和部署 receipt 分阶段记录。
