# ChatGPT Math 26.818.61809 运行启动验收

日期：2026-08-25

状态：`PASS_RUNTIME_LAUNCH_SMOKE`

## 已确认

- 从 staging 候选自身的可执行文件启动，没有使用 `open -a`。
- 使用独立 profile，避免相同 Bundle ID 把启动请求路由到旧副本。
- 出现真实标准窗口，应用身份为 `ChatGPT Math 26.818.61809 History`。
- renderer 加载 `app://-/index.html`。
- 候选版本为 26.818.61809，build 为 7019。
- 候选 ASAR SHA-256 为 `20af3332ab0fbf396eee9be3cde8e61e61025eca8287a1f0388fab03aa5c8e29`。
- 用户完成可见检查后明确反馈没有问题，并授权移除旧副本。

## 本轮未擅自执行

- 没有由代理发送测试消息。
- 没有修改应用设置。
- 没有读取聊天数据库，也没有在 receipt 中记录界面正文。
- 没有读取、暂停、恢复或修改 Computer History 状态。

## 证据边界

本状态证明候选能够从自己的可执行文件在隔离 profile 中真实启动，并记录用户的可见验收。它不等同于重新执行 26.818.41509 的完整运行矩阵。

以下项目本轮没有独立重跑，因此不写成已通过：

- 预览与发送后气泡的原文哈希、规范化组件子树哈希比较。
- 两类注释数学的运行时检查。
- 焦点、撤销、输入法 composition 和全部保护样例。
- Computer History 至少 315 秒暂停保护与恢复。

静态补丁、完整 ASAR、签名和 payload 范围的验证仍由本版本已有静态 receipt 负责。
