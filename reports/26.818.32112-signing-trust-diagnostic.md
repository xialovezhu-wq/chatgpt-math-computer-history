# ChatGPT Math 26.818.32112 签名与系统信任诊断

日期：2026-08-22

最终状态：`BLOCKED_KEYCHAIN_TRUST_ACTION_REQUIRED`

更新状态：`UPDATE_NOT_COMPLETE`

## 1. 诊断范围

本轮只执行代码签名 identity、证书链、Keychain 位置、私钥关联、trust settings、Code Signing policy、`codesign`、`spctl` 和窄范围 AMFI 日志的诊断。

本轮未修改 Keychain 或 trust settings，未导出私钥或 p12，未重做阶段 B–E，未修改四个已验证 ASAR entry，未改变候选 ASAR hash，未重新签名或部署候选应用，未继续 F–G–H 运行验收。

## 2. 证书链与 Keychain 位置

Leaf：

- 名称：`Codex Math Local Signing`
- SHA-256：`05543E7A6CD81E97489413AF9A96CC16C27376FC8B3723E57BE6E129ABAE8483`
- 证书位置：`login.keychain-db`
- System Keychain：不存在
- 对应私钥：存在于 `login.keychain-db`；`security find-identity -p codesigning -v` 对该 Keychain 返回 1 个有效 identity

Root：

- 名称：`Codex Math Local Root CA`
- SHA-256：`BBE6A65934BFFCCAECDEA135C82B5D0B2B3C989F9C9AB64873270D17BB97AE02`
- 证书位置：`login.keychain-db`
- System Keychain：不存在
- 对应私钥：未在 `login.keychain-db` 或 System Keychain 找到；对现有 leaf identity 签名和将 root 设为系统信任锚不是必需项

System Keychain 中没有上述 leaf、root 或 codesigning identity。

## 3. Identity 结果

`security find-identity -p codesigning -v` 结果：

- `Codex Math Local Signing`：1 个有效本地 codesigning identity。
- Apple Development：未找到。
- Developer ID Application：未找到。

旧数学副本和阶段 F staging 候选的外层链均为：

1. `Codex Math Local Signing`
2. `Codex Math Local Root CA`

TeamIdentifier 未设置。

## 4. Code Signing policy 与 trust settings

用户级 trust settings：

- 只有 `Codex Math Local Root CA` 一项。
- Policy OID 为 `Code Signing`。

管理员级 trust settings：

- 无。

System Keychain：

- 不包含 `Codex Math Local Root CA`。

Policy 评估：

- `security verify-cert -p codeSign`：在当前用户信任下成功，`TrustResultValue = 1`。
- `codesign` 的 `anchor trusted` requirement：通过。
- `codesign` 的 `anchor apple generic` requirement：本地链失败；官方 ChatGPT 通过。
- `spctl --assess --type execute`：本地签名 probe 被拒绝。

因此，用户级 Code Signing trust 可以让通用 Security.framework 评估和 `anchor trusted` 通过，但尚未形成当前 AMFI 接受的系统执行信任链。

## 5. 隔离签名启动 probe

在忽略的 `build/26.818.32112/signing-trust-probe/` 目录构建了最小 Mach-O app probe，并使用 `Codex Math Local Signing` 签名。

运行了两个变体：

1. 无 entitlement。
2. 原样使用旧数学副本的六项 Electron runtime entitlement。

两个变体的 `codesign --verify --deep --strict` 均通过。进程本身可以打印探针文本并返回 0，但两者都在同一次真实执行中留下 AMFI 错误 `-423`：`The file is adhoc signed or signed by an unknown certificate chain`。

因此不能将“进程返回 0”记为 AMFI 签名探针通过，也不能进入自动重签名分支。

## 6. 阻断判定

最终状态：`BLOCKED_KEYCHAIN_TRUST_ACTION_REQUIRED`。

现有本地 identity 和 leaf 私钥完整，但 root 只有用户级 Code Signing trust，不在 System Keychain；同时没有可用的 Apple Development 或 Developer ID Application identity。真实 probe 的 AMFI 记录仍将本地链判定为 unknown certificate chain。

## 7. 用户必须手动执行的唯一最小信任项

使用 Keychain Access，只将 root 公开证书 `Codex Math Local Root CA` 放入 System Keychain，并将该 root 的 `Code Signing` 信任设为 `Always Trust`。

不要移动或更改 leaf `Codex Math Local Signing`，不要移动或更改 leaf 私钥，不要改变其他 policy，不要更改 SIP、Gatekeeper 或 AMFI。

本轮没有执行该操作。

## 8. 人工修改后的最小 probe

人工修改后，先不签名或部署 ChatGPT 候选应用。只对已有隔离 probe 重签名，执行它，然后确认 `spctl` 不再拒绝且最近 AMFI 日志不再出现 `-423` 或 `unknown certificate chain`。

```zsh
probe='build/26.818.32112/signing-trust-probe/SigningTrustProbe.app'
codesign --force --sign 'Codex Math Local Signing' --timestamp=none "$probe"
codesign --verify --deep --strict --verbose=2 "$probe"
spctl --assess --type execute --verbose=4 "$probe"
"$probe/Contents/MacOS/SigningTrustProbe"
log show --last 1m --style compact --predicate '(process == "amfid" OR process == "kernel") AND eventMessage CONTAINS[c] "SigningTrustProbe"'
```

通过条件：静态验证通过，`spctl` 不再拒绝，probe 返回 0，且 AMFI 日志无 `-423`、`adhoc signed`或 `unknown certificate chain`。

## 9. 保留状态

- 官方 ChatGPT 保留，ASAR SHA-256 仍为 `128c748e313a7a630d689f9fa215724eb44fbea6e0a5d7990867370cf73d88d3`。
- 旧数学副本保留，ASAR SHA-256 仍为 `e3497261ea7fcc556ad9a2f829d361c1a593a86b698175ed6825b8c92e05adef`。
- 阶段 E 候选保留，SHA-256 仍为 `6ea7daa520796a215485fc2872b32da4c68a20ae3bc8295f2c758caa006c18e4`。
- 既有 blocked commit、失败报告和 receipt 未删除、未改写。
- 候选新版仍不在 `/Applications`。
- F–G–H 运行验收未继续，`UPDATE_COMPLETE` 未达到。
