# ChatGPT Math 26.818.32112 最新版部署结果

日期：2026-08-22

状态：`PASS_LATEST_MATH_COPY_DEPLOYED`

## 1. 实际结果

已将阶段 E 最终候选部署为：

`/Applications/ChatGPT-Math-26.818.32112-History.app`

安装版本：26.818.32112，build 6933。

阶段 E 候选与已安装 ASAR SHA-256 均为：

`6ea7daa520796a215485fc2872b32da4c68a20ae3bc8295f2c758caa006c18e4`

四个已验证 ASAR entry 没有重做或修改，候选 hash 没有变化。

## 2. 本轮修正

1. 候选构建脚本现在显式复用旧数学副本的 designated requirement。
2. 已安装新版与旧副本一致，均绑定 `Codex Math Local Signing` leaf 证书。
3. 候选验证器新增 designated requirement 等值检查。
4. 部署脚本使用新的独立回滚槽和 manifest，不覆盖此前失败证据。

## 3. 静态验证

- `codesign --verify --deep --strict`：PASS。
- designated requirement 与旧数学副本：PASS。
- 六项本地 Electron runtime entitlement：PASS。
- Info.plist 只有 `CFBundleDisplayName`、`CFBundleIdentifier` 和 `ElectronAsarIntegrity` 三项批准变化。
- payload 只有 Info.plist、外层主可执行文件、app.asar 和外层 CodeResources 四类批准变化。
- 已安装 app.asar 与阶段 E 候选 hash：PASS。

## 4. 真实运行验证

新版 staging 使用 build 隔离 `--user-data-dir` 真实启动：

- 主进程、renderer 和 service 进程正常。
- 完整 ChatGPT/Codex 窗口可访问。
- 本地 app-server initialize handshake 成功并进入 connected。
- 已认证 ChatGPT 账户存在。
- 项目、任务、分支和编辑器表面正常。
- 打开现有数学对话后，用户消息气泡中的积分上下限、分式、指数和行内数学均可见地渲染为数学公式。

已安装新版使用独立 `--user-data-dir` 再次真实启动：

- 已安装路径的主进程正常。
- 窗口和 renderer 正常。
- app-server 连接成功。
- 已认证账户存在。

## 5. 运行边界

官方主应用、官方 companion 和数学副本同时运行时，仍会观察到 remote app-server HTTP 409、同一任务 active writer 等既知双实例冲突。

长期正确形态仍是：

- 数学副本作为唯一可见主界面。
- 官方 Computer History companion 使用独立 Shared Config profile。
- 不在两个可见主界面中同时操作同一任务。

## 6. 保留状态

- 官方 ChatGPT 没有修改。
- 旧数学副本 26.814.41407 保留。
- 新数学副本 26.818.32112 已安装并使用隔离 profile 运行。
- 此前 blocked commit、报告和 receipt 保留，未改写。
- 本轮未重做完整 History 300 秒周期验收，因此不将原始 F–G–H 全项状态写为 `UPDATE_COMPLETE`。
