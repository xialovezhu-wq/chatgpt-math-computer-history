# ChatGPT Math 26.818.41509 运行时验收

日期：2026-08-24

状态：`PASS_RUNTIME_CANDIDATE`

## 运行环境

- 候选应用在隔离 profile 中真实启动。
- 实际 Electron 窗口、renderer、app-server 连接和已登录账户均可用。
- 候选 ASAR SHA-256 为 `19095005e8ea6862aa5d04ddf92f60ef605ea38fb34e3dac4f05653a6c9b8a48`。

## 预览与用户气泡严格对齐

使用同一份测试原文比较预览和发送后的用户气泡：

- 两边原文长度均为 192。
- 两边原文 SHA-256 均为 `47a0536637761ce1f8a7af271ff27a476893c0c574a3e33acd22e71b1a903cdd`。
- 规范化时仅忽略临时的 `data-selected-text-overlay-target` 属性。
- 两边 `user-formatted-text` 内部 DOM SHA-256 均为 `98c9043d26a1cd54fe9bb4941f4e6cf41e5cb047a104b547e87269c7dd5dea66`。
- 预览和气泡均生成 1 个独立 KaTeX 公式节点，未出现 chunk 加载错误。

这项比较证明的是共享组件内部一致；外层 composer 与用户气泡容器不参与比较。

## 数学保护与错误退回

- 代码 span 和 fenced code 保持代码，其中的数学样式文本没有被 tokenizer 抢占。
- 货币、Shell 变量和 URL 保持原文。
- 保护样例只生成 1 个预期数学节点，没有额外数学节点。
- 错误 LaTeX 只产生 1 个局部错误节点，预览不为空，页面不崩溃，仍可返回源代码。

## 编辑器不变性

- 进入、退出预览后，controller 原文和可见源文相同。
- 返回源代码后焦点恢复。
- 输入一次再撤销，文本精确恢复。
- composition 期间点击预览未进入预览态。
- 预览切换生成的正文 transaction 数为 0。
- 验收后已把“纯文本编辑器”设置恢复为关闭。

## Computer History

- 通过候选应用界面显式暂停。
- 每 45 秒采样一次，共 8 次，覆盖至少 315 秒。
- 8 次状态均为 `paused`，伴随主进程始终为 1，未被自动恢复。
- 300 秒周期后已观察到手动暂停 latch。
- 随后通过候选应用界面显式恢复，最终状态为 `running`，伴随主进程为 1，latch 已清除。

## 边界

- 未记录验收消息正文或 Computer History 内容，只记录长度、哈希、节点计数和状态。
- 多个可见主应用同时运行仍可能触发既知 remote 409，因此不声称该多实例边界已消失。
- 验收结束后已关闭候选应用和调试端口。
