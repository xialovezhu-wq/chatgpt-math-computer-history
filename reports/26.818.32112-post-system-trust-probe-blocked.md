# ChatGPT Math 26.818.32112 系统信任后签名探针结论

日期：2026-08-22

最终状态：`BLOCKED_SIGNING_IDENTITY_REQUIRED`

更新状态：`UPDATE_NOT_COMPLETE`

## 1. 结论修正

先前的 `BLOCKED_KEYCHAIN_TRUST_ACTION_REQUIRED` 是一个可验证的中间判断，不是最终根因。用户完成唯一的系统信任操作后，该判断被真实 probe 否定。

当前已确认：

- `Codex Math Local Root CA` 存在于 System Keychain。
- System Keychain 中的 root SHA-256 为 `BBE6A65934BFFCCAECDEA135C82B5D0B2B3C989F9C9AB64873270D17BB97AE02`，与原链完全一致。
- 管理员级 trust settings 已出现该 root，Policy OID 为 `Code Signing`。
- `Codex Math Local Signing` 仍是唯一有效 codesigning identity。
- Apple Development identity 仍未找到。
- Developer ID Application identity 仍未找到。

## 2. 系统信任后的最小 probe

管理员级 Code Signing trust 落盘后，重新用 `Codex Math Local Signing` 签署隔离无 entitlement Mach-O app probe。

结果：

- `codesign --verify --deep --strict`：PASS。
- 进程直接执行：打印 `SIGNING_TRUST_PROBE_OK`，返回 0。
- `spctl --assess --type execute`：rejected，exit 3。
- 同一次真实执行的 AMFI 日志：error `-423`，`The file is adhoc signed or signed by an unknown certificate chain`。

因此，进程返回 0 仍不能被记为 AMFI probe 通过。用户级与管理员级 Code Signing trust 都已存在时，当前 AMFI 仍不接受本地 CA 链。

## 3. 最终阻断判定

最终状态：`BLOCKED_SIGNING_IDENTITY_REQUIRED`。

当前不是缺少本地 leaf、本地 leaf 私钥或本地 root trust。这三项都已存在，但真实 AMFI probe 仍失败。

需要以下任一已由 macOS AMFI 接受的 Apple 签名 identity：

1. `Developer ID Application`：对独立 macOS `.app` 的长期本机与分发签名更直接，是当前候选的首选。
2. `Apple Development`：可作为开发探针 identity，但仍需验证 provisioning、Team 和 nested entitlement constraint。

重新创建另一条本地自签 CA 链没有证据能解决当前阻断，不应作为下一步。

## 4. 未执行的操作

因为 Apple identity 仍不存在，本轮没有：

- 重新签名阶段 E 候选应用。
- 修改 nested component 签名或 entitlement。
- 部署新版到 `/Applications`。
- 执行数学 UI、annotation UI、History、companion 唯一性或 300 秒周期验收。
- 修改四个 ASAR entry 或候选 ASAR hash。
- 删除旧数学副本或改写既有 blocked commit、报告和 receipt。

## 5. 保留状态

- 官方 ChatGPT ASAR SHA-256：`128c748e313a7a630d689f9fa215724eb44fbea6e0a5d7990867370cf73d88d3`。
- 旧数学副本 ASAR SHA-256：`e3497261ea7fcc556ad9a2f829d361c1a593a86b698175ed6825b8c92e05adef`。
- 阶段 E 候选 SHA-256：`6ea7daa520796a215485fc2872b32da4c68a20ae3bc8295f2c758caa006c18e4`。
- 新版候选仍不在 `/Applications`。
- `UPDATE_COMPLETE` 未达到。
