# ChatGPT Math 26.818.61809 Staging 静态签名验证

日期：2026-08-25

状态：`PASS_SIGNING_PAYLOAD_STATIC`

## 候选结果

staging 候选已从官方 26.818.61809 完整复制、替换版本锁定 ASAR 并完成本地签名。候选位于仓库忽略的 `build/26.818.61809` 目录，没有安装到 `/Applications`。

- Version：26.818.61809
- Build：7019
- Final ASAR SHA-256：`20af3332ab0fbf396eee9be3cde8e61e61025eca8287a1f0388fab03aa5c8e29`
- Final header SHA-256：`624b1fe2cd85d9a2d3877f9b275112e9e35e718abbb9e500231e736350876f3e`
- 回滚副本：26.818.41509，ASAR SHA-256 `19095005e8ea6862aa5d04ddf92f60ef605ea38fb34e3dac4f05653a6c9b8a48`

## 通过项

- staging ASAR 与静态最终候选逐字节一致。
- `ElectronAsarIntegrity` 使用已冻结的 final header hash。
- Info.plist 只有 Display Name、Bundle ID 和 Electron integrity 三项批准差异。
- 相对官方 payload 只有 Info.plist、外层主可执行文件、app.asar 和外层 CodeResources 四类批准差异。
- `codesign --verify --deep --strict` 通过。
- designated requirement 与 26.818.41509 回滚副本一致。
- 六项 Electron 运行权限与旧副本批准集合完全一致，没有 App Group、Keychain group、Team identifier 或 app sandbox entitlement。
- 新旧两个本地签名副本的 Gatekeeper assessment 退出状态同为 3，保持既有本地签名边界；该结果不代表 Apple 公证分发通过。
- 官方应用和 26.818.41509 回滚副本 ASAR 哈希在 staging 构建后保持不变。

## 尚未执行

- 没有启动 staging 或任何已安装应用。
- 没有发送测试消息或读取现有聊天。
- 没有调用、暂停、恢复或修改 Computer History。
- 没有部署到 `/Applications`，也没有运行 deploy 或 rollback 脚本。
- 没有删除旧副本、创建部署 tag 或推送 GitHub。

下一阶段仍需用户单独授权真实运行验收。
